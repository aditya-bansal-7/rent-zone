import SwiftUI

// This file is kept temporarily because Xcode still has a reference to it in the project file.
// Since the Language feature is removed, this view is no longer used anywhere in the app.
struct LanguageSettingsView: View {
    var body: some View {
        Text("Language Settings Removed")
    }
}
